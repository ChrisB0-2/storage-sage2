apiVersion: 1

providers:
  - name: "Storage-Sage"
    orgId: 1
    folder: "Storage-Sage"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
